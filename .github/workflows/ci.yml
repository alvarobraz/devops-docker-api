name: CI

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build:
    name: 'Build and Push'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - run: npm install
      - run: npm run test

      - name: Generate tag
        id: generate_tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.0.0
        with:
          role-to-assume: 'arn:aws:iam::530482047728:role/ecr-role'
          aws-region: us-east-2

      - name: Login to AWS ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # - name: Login into the container registry
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Build and push
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     file: ./DockerFile
      #     push: true
      #     tags: |
      #       ${{ secrets.DOCKERHUB_USERNAME }}/devops-docker-api:${{ github.sha }}
      #       ${{ secrets.DOCKERHUB_USERNAME }}/devops-docker-api:latest
      #     secrets: |
      #       GIT_AUTH_TOKEN=${{ secrets.GITHUB_TOKEN }}

      # - name: Build docker image
      #   run: docker build -t alvarobraz/devops-docker-api:${{ steps.generate_tag.outputs.sha }} -f DockerFile .

      # - name: Push image
      #   run: docker push alvarobraz/devops-docker-api:${{ steps.generate_tag.outputs.sha }}
